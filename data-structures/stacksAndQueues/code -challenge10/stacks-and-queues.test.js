'use strict';

const Stack = require('./stacks-and-queues.js');
const Queue = require('./stacks-and-queues.js');


let stackClass = Stack();
let queueClass = Queue();

describe('stacks', () => {
  it('should add a node to the stack', () =>{
    stackClass.push('a');
    stackClass.push('b');
    stackClass.push('c');
    expect(stackClass).toBe()
  })
})

describe('Queues', () => {
  it('should add a node to the queue', () =>{
    expect().toBe()
  })
})
